//= require react
//= require react_ujs
//= require flux
//= require underscore
//= require backbone
//
//= require_tree ./components
//= require_tree ./stores
//= require_tree ./constants
//= require_self

// Create stuff we need for the app.
const PlaceDispatcher = new Flux.Dispatcher();
const PlaceStore      = new _PlaceStore();

// Render the main component.
React.render(
  <Map accessToken="<%= ENV.fetch('MAPBOX_PUBLIC_TOKEN') %>"
       mapId='mapbox.streets'
       initialZoom={ <%= MapHelper::TOWN_LEVEL_ZOOM %> }
   />,
  document.getElementById('app')
);
