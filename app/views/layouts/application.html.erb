<!DOCTYPE html>
<html>
  <head>
    <title>
      Places <%= " | #{yield :page_title}" if content_for?(:page_title) %>
    </title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class='container'>
      <%= yield %>
    </div>

    <%= javascript_include_tag 'application' %>
  </body>
</html>
